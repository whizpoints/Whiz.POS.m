const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.business.findFirst().then(b => { let parsedSettings = {}; try { if (typeof b.settings === 'string') parsedSettings = JSON.parse(b.settings); } catch(e){} console.log(parsedSettings.address); console.log(parsedSettings.phone); }).finally(()=>process.exit(0));
